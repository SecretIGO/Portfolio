{
    "version": 2,
    "framework": "vite",
    "buildCommand": "vite build",
    "devCommand": "vite",
    "outputDirectory": "dist",
    "ignoreCommand": "git diff --quiet HEAD^ HEAD || echo 'changed'"
}